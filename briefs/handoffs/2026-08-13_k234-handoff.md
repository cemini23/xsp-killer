# Handoff — K234 Moontower high-IV collar shopping (log-only)

**Lane:** hard | **Repo:** `/opt/xsp-killer` `main`
**Plan:** Cursor | **Implement:** claude-ds (`--dangerously-skip-permissions`) — Grok CLI out of usage
**Debug after:** GPTSOL in Cursor (separate)

## Hard stops
- No LIVE Discord / LIVE_* flips; no secrets
- **Not** an auto-LIVE collar strategy — operator GO required
- Do not auto-place collars; no strategy/gate flips
- Single-name examples (SNDK-class) illustrative only — map carefully before any index/XSP analog
- GREEN `playbook_snapshot` gates unchanged
- Pair as hedge vocabulary with existing high-vol regime gates (awareness only)

## Brief
`/opt/cemini/briefs/xsp-k234-moontower-collar-shopping.md` (staged under `briefs/`)

## Skip
Harness K274–K277; OOD routes; K231–K233 shipped

## Plan (Cursor) — EXECUTE

### Goal
Log-only `k234:` after `k233`. Overnight ends:
`keep_tight_vs_k233: true` + `no_posture_change_on_teaser: true`.

### 1) YAML — append after k233 in `config/k155_operator_notes.yaml`

```yaml
# K234 — Moontower high-IV collar shopping hedge vocabulary (log-only; extends K233).
k234:
  version: "2026-08-13"
  moontower_high_iv_collar_shopping:
    flag: high_iv_collar_hedge_vocabulary
    otm_calls_rich_vs_otm_puts_when_iv_elevated: true
    call_overwrite_finances_puts_near_zero_cost_collar: true
    rates_lifting_forwards_reinforce_call_richness: true
    collar_rr_improves_when_overwrite_finances_meaningful_put: true
    pair_with_existing_high_vol_regime_gates: true
    hedge_shopping_vocabulary_only: true
    illustrative_single_name_sndk_class_only: true
    map_carefully_before_index_xsp_analog: true
    no_auto_place_collars: true
    operator_go_required: true
    not_auto_live_collar_strategy: true
    note: "Moontower high-IV collar shopping: when IV elevated, risk-neutral pricing often makes OTM calls rich vs OTM puts — natural longs can finance puts via call overwrites (near zero-cost collars); rates lifting forwards reinforce call richness — hedge-shopping vocabulary under high-vol regimes paired with existing XSP vol-regime gates; not an auto-LIVE collar strategy; no auto-place; operator GO required; SNDK-class examples illustrative only — map carefully before any index/XSP analog"
  lane_a_overnight:
    keep_tight_vs_k233: true
    no_posture_change_on_teaser: true
    note: "Keep overnight tight vs K233 Fed Speaks / Wu derivatives stack; no posture change on Moontower collar vocabulary alone"
  constraints:
    no_integral_solver: true
    no_strategy_code: true
    no_diffusion_vendor: true
    no_cf_workers_vendor: true
    no_live_auto_calibrator: true
    no_neural_operator_serving: true
    no_live_pricer_wire: true
    no_tabular_llm_prediction: true
    no_prod_manip_detector: true
    no_auto_lane_size_change: true
    no_auto_watches_json_writes: true
    no_order_bots: true
    no_lane_retune_on_paper_forecasts: true
    no_auto_hl_perp_size_change: true
    no_auto_place_collars: true
    not_auto_live_collar_strategy: true
    playbook_snapshot_gates_unchanged: true
    note: "Honors brief: hedge vocabulary only; not auto-LIVE collar strategy; no auto-place collars; operator GO required; GREEN playbook_snapshot gates unchanged; weather notes only"
```

Merge keys (k234 last): `k234_version`, `moontower_high_iv_collar_shopping`, `lane_a_overnight`, `constraints`.

### 2) Code `xsp_killer/macro_weather_notes.py`
- `load_k234_notes` after `load_k233_notes`
- `build_monitor_macro_weather_extras`: param `k234_notes`, load + merge **after** k233; docstring through K234

### 3) Tests `tests/test_k155_macro_weather_notes.py`
- Import `load_k234_notes`
- Add load / includes / from-prod tests mirroring k233
- Rewrite **all from-prod overnight** asserts `keep_tight_vs_k232` → `keep_tight_vs_k233` (k234 wins)
- Isolated k233 fixture may still assert `keep_tight_vs_k232`
- `test_run_monitor_attaches_macro_weather_extras`: `k234_version == "2026-08-13"` + collar content key + overnight `keep_tight_vs_k233` + `no_auto_place_collars` + `not_auto_live_collar_strategy` + `operator_go_required`

### 4) Ship verification (do not commit unless asked)
```
python3 -m pytest tests/test_k155_macro_weather_notes.py -q
```
Smoke: `build_monitor_macro_weather_extras` → k234_version + keep_tight_vs_k233 + no_auto_place_collars.

When done print: DONE_K234 and pytest summary. Do **not** flip LIVE_*. Do **not** commit/push unless explicitly asked.
